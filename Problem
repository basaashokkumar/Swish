#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

int main()
{
    ll p,d,temp;
    cin>>p>>d;
    ll ans = p;
    while(1)
    {
        temp = (p*d)/100;
        if(temp==0)
            break;
        p-=temp;
        ans+=p;
    }
    cout<<ans<<'\n';
}
